<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppMovilFE.Master"
             Title="MOVILF@CT">
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand" WidthRequest="150" Padding="30">
            <ListView x:Name="listView"  VerticalOptions="FillAndExpand" 
                      SeparatorVisibility="Default"
                      ItemSelected="ListView_OnItemSelected">
                <ListView.ItemTemplate  >
                    <DataTemplate>
                        <ImageCell   TextColor="Navy" Text="{Binding Title}" ImageSource="{Binding IconSource}"  />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>